{
  "name": "telerin",
  "description": "Terminal/tileset renderer for web based games.",
  "version": "0.0.0",
  "main": "dist/index.js",
  "repository": "git@github.com:danprince/silmarils.git",
  "license": "MIT",
  "scripts": {
    "dev": "./scripts/dev",
    "build": "./scripts/build",
    "typecheck": "tsc --noEmit",
    "declarations": "tsc --declaration --emitDeclarationOnly",
    "prepublishOnly": "yarn typecheck && yarn build"
  },
  "devDependencies": {
    "esbuild": "^0.14.27",
    "typescript": "^4.6.2"
  },
  "files": [
    "dist",
    "index.ts"
  ]
}
